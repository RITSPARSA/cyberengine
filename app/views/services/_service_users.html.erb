<% service = @service if @service %>
<% users = service.users %>

<table class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>Username</th>
      <th>Password</th>
    </tr>
  </thead>
  <tbody>
    <% if users.count == 0 %>
      <tr>
        <td colspan="2">No Users</td>
      </tr>
    <% else %>
      <% users.each do |user| %>
        <tr>
          <td><%= user.username %></td>
          <td><%= user.password %></td>
        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>
