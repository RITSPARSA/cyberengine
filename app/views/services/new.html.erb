<div class="page-header">
  <table>
    <tr>
      <td>New <%= @team.alias %> Service</td>
      <td>
        <%= submit_tag 'Create Service', onclick: "$('#new_service').trigger('submit')", class: 'btn btn-primary' %>
        <%= link_to 'Cancel', team_services_path(@team), class: 'btn' %>
      </td>
    </tr>
  </table>
</div>

<%= render partial: 'form' %>
