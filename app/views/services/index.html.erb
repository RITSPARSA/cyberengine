<div class="page-header">
  <table>
    <tr>
      <td><%= @server.name %> Services</td>
      <td>
        <%= link_to "New Service", new_team_server_service_path(@team,@server), class: 'btn btn-primary' if can? :new, Service %>
     </td>
    </tr>
  </table>
</div>

<%= render partial: 'server', locals: {team: @team, server: @server } %>
<%= render partial: 'checks/modal' %>
<%= render partial: 'properties/modal' %>
<%= render partial: 'users/modal' %>
