<% users = @service.users %>
<% if users.count == 0 %>
  <p>No users</p>
<% else %>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Username</th>
        <th>Password</th>
      </tr>
    </thead>
    <tbody>
      <% users.each do |user| %>
        <tr>
          <td><%= user.username %></td>
          <td><%= user.password %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
