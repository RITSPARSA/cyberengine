<div class="page-header">
  <table>
    <tr>
      <td><%= @team.alias %> Servers</td>
      <td><%= link_to "New Server", new_team_server_path(@team), class: 'btn btn-primary' if can? :new, Server %></td>
    </tr>
  </table>
</div>

<%= render partial: 'teams/team_servers' %>
